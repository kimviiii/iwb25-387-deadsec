# name: CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   build-and-test:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4
      
#     - name: Set up Java 17
#       uses: actions/setup-java@v4
#       with:
#         distribution: 'temurin'
#         java-version: '17'
        
#     - name: Install Ballerina
#       run: |
#         wget https://dist.ballerina.io/downloads/2201.10.2/ballerina-2201.10.2-swan-lake-linux-x64.deb
#         sudo dpkg -i ballerina-2201.10.2-swan-lake-linux-x64.deb
      
#     - name: Check Ballerina version
#       run: bal version
      
#     - name: Build the project
#       run: bal build
#       working-directory: backend
      
#     - name: Run tests
#       run: bal test --offline
#       working-directory: backend
